#include <stdio.h>
#include <stdlib.h>
#include <time.h>

#define TAM 10

int main() {
  int v[TAM], selecao[TAM], bolha[TAM];
  srand(time(NULL));

  for (int i = 0; i < TAM; i++) {
    v[i] = min + ( rand() % (max+1) );
  }
  
  selecao[TAM ]
}